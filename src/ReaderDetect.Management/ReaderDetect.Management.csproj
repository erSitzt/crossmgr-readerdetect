<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <RootNamespace>ReaderDetect.Management</RootNamespace>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <!-- SSH.NET is the only reason this is a separate project: the discovery
         library stays dependency-free for the host application. -->
    <PackageReference Include="SSH.NET" Version="2026.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../ReaderDetect/ReaderDetect.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="ReaderDetect.Tests" />
  </ItemGroup>

</Project>
